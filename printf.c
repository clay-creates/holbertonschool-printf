#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/***/

form forms[] = {
    {'c', print_char},
    {'s', print_str},
    {'d', print_int},
    {NULL, NULL}};

int i = 0;
int tracker = 0;

while (format != NULL)
{
    if (format == '%' {
            format++;
            (*get_func(*format)(args));
        })
}